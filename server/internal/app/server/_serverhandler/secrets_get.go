package serverhandler

import (
	"context"

	"github.com/adamlouis/squirrelbyte/server/pkg/model"
)

func (a *apiHandler) GetSecret(ctx context.Context, key string) (*model.Secret, error) {
	return nil, nil
}
